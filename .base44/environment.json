{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for AI-powered features. Obtain from Google AI Studio (aistudio.google.com) → Get API Key.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    },
    {
      "name": "APP_URL",
      "description": "The public URL where this app is hosted. Used for self-referential links and OAuth callbacks.",
      "requiredAtBoot": false
    }
  ]
}
